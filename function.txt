CREATE FUNCTION get_team_goals(@team_id int)
RETURNS int
AS
BEGIN
    DECLARE @goals int;

    SELECT @goals = SUM(goal_home + goal_away)
    FROM match
    WHERE team_id = @team_id;

    RETURN @goals;
END


SELECT dbo.get_team_goals(1);


